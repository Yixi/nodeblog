//- nav bar

ul.nav.nav-pills.nav-stacked
    li
        a(title='homepage',href="/") home
    - if (locals.user)
        li
            a(title="post",href="/post") post
        li
            a(title="sign out",href="/logout") sign out
    - else
        li
            a(title='signin',href='/login') login
        li
            a(title='register',href='/reg') register
